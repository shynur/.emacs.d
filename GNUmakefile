#! /bin/make -f
### ~shynur/.emacs.d/GNUmakefile

.PHONY: clean
clean:
	rm --force --recursive .shynur/
	rm --force             README.html
	rm --force             docs/*.html
	rm --force --recursive elpa/

# Local Variables:
# coding: utf-8-unix
# End:
